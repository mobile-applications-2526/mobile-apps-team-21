# Base application configuration
spring.application.name=EatUp-Team21

# Active profile - set to 'prod' on Azure via environment variable
spring.profiles.active=${SPRING_PROFILES_ACTIVE:}

# MongoDB Configuration (local dev uses localhost, Azure uses env var)
spring.data.mongodb.uri=${SPRING_DATA_MONGODB_URI:mongodb://localhost:27017}
spring.data.mongodb.database=${SPRING_DATA_MONGODB_DATABASE:Eat-Up}

# JWT Configuration
spring.security.jwt.secret=${SPRING_SECURITY_JWT_SECRET:02ca164a0335a567e1baadf2b4141b7402fa7ae3rekmgndfkjmnlq5dsf5sd4f6f54g5s}
spring.security.jwt.expiration-ms=${SPRING_SECURITY_JWT_EXPIRATION_MS:86400000}

# Server Configuration
server.port=${PORT:8080}
